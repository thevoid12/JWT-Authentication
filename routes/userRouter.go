package routes

import (
	// "jwtAuth/handler"
	// "jwtAuth/middleware"

	"github.com/gin-gonic/gin"
)

func UserAuthRoutes(incomingRoutes *gin.Engine) {
	// incomingRoutes.Use(middleware.Authenticate())
	// incomingRoutes.GET("/users", handler.GetAllUsers())
	// incomingRoutes.GET("/users/:user_id", handler.GetUser())
}
